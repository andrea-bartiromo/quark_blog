# Verifica internal-only PR #522 — Social Workspace admin v1 (Prompt 111-115)

`feat/social-workspace-admin-v1` (#522) è stata aperta da questa stessa
sessione (footer "Generated by Claude Code",
`session_011Q3CxvEiFGx4kJomrCuRno` — la sessione corrente) in una parte
precedente di questa conversazione. Il perimetro di questo prompt —
"verifica internal-only" — è quindi lo stesso spirito della revisione
critica della PR #533 (Prompt 101-105): controllare le proprie stesse
affermazioni con nuove prove dirette, non ripeterle per fiducia.

## Metodo

Stesso metodo di #507/#510/#511: stato/CI via API GitHub, `git merge-tree`
locale contro `main` attuale, esecuzione locale indipendente dei test in
un checkout separato. In piu', per il perimetro specifico "internal-only"
di questo prompt: verifica diretta nel codice (non nei test) che nessuna
risorsa esterna sia mai caricata dalla preview, e che nessun
comando/scheduler consumi mai una bozza "scheduled".

## Stato

Aperta, non draft, **7/7 check GitHub verdi** (incluso "Chromium public
regression" — il browser spec `tests/browser/social-drafts.spec.js`,
scritto ma non eseguibile localmente per assenza di `node_modules`
secondo la PR, risulta comunque eseguito e verde in CI).
`mergeable_state: clean`.

**Attenzione alla lettura del diff**: un `git diff main branch` diretto
mostra migliaia di righe "rimosse" — fuorviante, perché il branch della
PR è basato su uno SHA di `main` (`a094e7e`) molto piu' vecchio
dell'attuale, e quelle righe sono semplicemente file aggiunti da `main`
DOPO quel punto, non una cancellazione della PR. Il confronto corretto
(`git diff main...branch`, tre punti, dal punto di divergenza) mostra
cio' che la PR realmente cambia: **33 file, 3474 inserzioni, 0
cancellazioni** — puramente additivo, combacia esattamente con quanto
dichiarato nel corpo della PR. **Merge locale contro `main` attuale: 0
conflitti.**

## Verifica indipendente delle affermazioni "non fa" della PR

Eseguiti localmente (checkout separato, mai un branch tracciato): **127
test verdi, 222 assertion** (la PR dichiarava 121 — piu' che sufficiente,
nessuna discrepanza preoccupante).

Riletti direttamente i test che sostengono le affermazioni piu' forti
della PR, non solo il loro nome:

- `test_no_job_is_ever_dispatched_by_workspace_actions` — `Queue::fake()`
  reale, poi store→update→transition verso "reviewed", poi
  `Queue::assertNothingPushed()`. Non un mock vuoto: la sequenza esercita
  azioni reali del workspace.
- `test_no_outbound_http_call_is_ever_made_by_workspace_actions` —
  `Http::fake()` reale, stessa sequenza, `Http::assertNothingSent()`.
- `test_social_publications_table_is_never_written_by_workspace_actions`
  — `assertDatabaseCount('social_publications', 0)` dopo le stesse
  azioni.
- Un test dedicato (`SocialDraftHttpTest.php`, riletto per intero)
  conferma che titolo, cover, alt, categoria, stato e `published_at`
  dell'articolo sorgente sono byte-per-byte identici prima e dopo ogni
  azione del workspace.

## Verifica "internal-only" oltre i test dichiarati

- `grep` diretto su `resources/views/components/admin/social-draft-preview.blade.php`
  per `<iframe`, `<script`, `facebook.com`, `linkedin.com`, `cdn.`: **zero
  corrispondenze**. La preview è genuinamente server-rendered, nessuna
  risorsa esterna referenziata in alcun modo.
- `grep` di `SocialDraft::` in `app/Console/` e `routes/console.php`:
  **zero corrispondenze**. Nessuno scheduler o comando consuma mai una
  bozza allo stato "scheduled" — lo stato esiste nella state machine ma
  non è collegato a nessuna azione automatica, coerente con
  `docs/SOCIAL_WORKSPACE_PROVIDER_FUTURE_CHECKLIST.md`, dichiarato
  esplicitamente "puramente progettuale, nessun punto implementato".

## Esito

Ogni affermazione centrale della PR ("nessuna chiamata esterna, nessuna
scrittura su `social_publications`, nessuna modifica all'articolo
sorgente, nessuna risorsa esterna nella preview") verificata con prove
dirette in questa sessione, non solo ereditata dalla descrizione della
PR stessa. Nessun problema trovato. Nessuna modifica al branch della PR,
nessun merge eseguito, nessuna modifica a produzione.
